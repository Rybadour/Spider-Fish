# build

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(SpiderFish CXX)

INCLUDE_DIRECTORIES(include)

SET(PROJECT_LIB_DIR ${PROJECT_SOURCE_DIR}/lib)

file(GLOB LIBS ${PROJECT_LIB_DIR}/*.lib)

ADD_SUBDIRECTORY(src bin)