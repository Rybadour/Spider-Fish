# src

FILE(GLOB_RECURSE SPIFI_SRCS *.cpp)
FILE(GLOB_RECURSE SPIFI_HEADERS ${PROJECT_SOURCE_DIR}/src/*.h)

ADD_EXECUTABLE(spifi_game ${SPIFI_SRCS} ${SPIFI_HEADERS})
TARGET_LINK_LIBRARIES(spifi_game ${LIBS})